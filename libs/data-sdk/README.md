# data-sdk

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build data-sdk` to build the library.

## Running unit tests

Run `nx test data-sdk` to execute the unit tests via [Vitest](https://vitest.dev/).
